import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'brett.technology',
  appName: 'sevents',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
